<configuration>
    <springProfile name="prod">
        <appender name="CLOUDWATCH" class="com.vimainsurance.vimaadmin.util.CloudWatchAppender">
            <logGroupName>VimaBackendLogs</logGroupName>
            <logStreamName>VimaBackendStream</logStreamName>
            <region>ap-south-1</region>
            <!-- F-02: Credentials removed. CloudWatchAppender now reads from the AWS DefaultCredentialsProvider chain
                 (EC2 instance role / ECS task role / SSO / env vars). The empty values let the appender skip the
                 explicit-credential branch and fall back to the SDK default. -->
            <accessKeyId>${VIMA_CLOUDWATCH_ACCESS_KEY:-}</accessKeyId>
            <secretKey>${VIMA_CLOUDWATCH_SECRET_KEY:-}</secretKey>
            <layout class="com.vimainsurance.vimaadmin.logging.MaskingPatternLayout">
                <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} [correlationId:%X{correlationId}] - %msg%n</pattern>
            </layout>
        </appender>

        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <layout class="com.vimainsurance.vimaadmin.logging.MaskingPatternLayout">
                <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </layout>
        </appender>

        <root level="INFO">
            <appender-ref ref="CLOUDWATCH"/>
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="!prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <layout class="com.vimainsurance.vimaadmin.logging.MaskingPatternLayout">
                <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </layout>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
</configuration>
