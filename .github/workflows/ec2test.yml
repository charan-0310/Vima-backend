name: Test EC2 Connection

on:
  workflow_dispatch:

jobs:
  test-ec2-ssh:
    runs-on: self-hosted
    steps:
      - name: Test SSH connectivity to EC2
        run: |
          echo "✅ Connected to EC2"
          hostname
          whoami
          uptime
          EOF
     
